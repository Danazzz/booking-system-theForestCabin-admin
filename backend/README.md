# Admin Backend Folder

This folder is kept as a legacy/reference backend from the early admin project.

The production booking system should use the shared backend instead:

```text
forestCabin-booking/backend
```

Use this folder only if you intentionally need to inspect or migrate old admin logic. Do not deploy it as a separate production backend for The Forest Cabin booking flow.
